// No hooks needed for v1 — data arrives fully resolved via chat metadata.
// Reserved for future client-side interaction (e.g. live incident status
// polling), mirroring how src/features/navigation/hooks/ started empty.
export {};
